query post($slug: String!) {
    post(slug: $slug) {
        content {
            body
        }
        title
        date
        slug
        coverImage
        description
    }
}

query slugs {
    posts {
        slug
    }
}
